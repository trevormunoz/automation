---
  - name: dump local wordpress db for backup
    hosts: local
    user: vagrant
    tasks:
      - name: dump db to wordpress-local.sql
        mysql_db: state=dump name={{ db.name }} target=/vagrant/data/wordpress-local.sql
        sudo: yes
